# OpenSeeFace Server Requirements
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
numpy>=1.24.0
opencv-python>=4.8.0
onnxruntime>=1.16.0
Pillow>=10.0.0
pyaudio>=0.2.13

# Note: pyaudio may require system dependencies:
# macOS: brew install portaudio
# Ubuntu: sudo apt-get install portaudio19-dev python3-pyaudio
# Windows: pip install pipwin && pipwin install pyaudio

